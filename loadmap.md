[작업 지시서] 금융거래조회서 OCR 엔진 고도화 및 중장기 로드맵
1. 개요 및 목적
현재의 단순 텍스트 기반 PaddleOCR 방식은 금융 문사의 촘촘한 표 구조와 노이즈가 섞인 이미지에서 텍스트 오인식 및 컬럼 밀림 현상이 발생하고 있습니다. 본 지시서는 이를 해결하기 위해 표 구조 인식 엔진(PP-Structure)을 도입하고, 보안이 강화된 지능형 보정(Local SLM) 단계를 거쳐 최종적으로 기술 스택의 피벗 여부를 결정하는 것을 목적으로 합니다.
2. 단계별 추진 계획 (Roadmap)
Phase 1: PP-Structure 기반 엔진 전환 (핵심 과제)
 * 목표: 단순 텍스트 검출에서 벗어나 표(Table)의 행/열 구조를 직접 추출하는 구조로 변경.
 * 주요 작업:
   * engine.py 내 PaddleOCR 클래스를 PPStructure로 교체.
   * Layout Analysis를 활성화하여 문서 내 불필요한 영역(90%)을 배제하고 **표 영역(Table Zone)**만 집중 처리.
   * 표 영역에 대해 SLANet 모델을 적용하여 Cell 단위의 HTML/JSON 데이터 확보.
Phase 2: 정밀 검증 및 벤치마킹
 * 목표: PP-Structure 도입 후 실제 성능 향상 폭을 정량적으로 측정.
 * 주요 작업:
   * label_bank_audit_letter.csv를 기준으로 추출 데이터의 정확도(WER, CER) 비교 테스트 수행.
   * 표 인식률: 전체 이미지 중 표 구조가 깨지지 않고 추출된 비율 측정.
   * 텍스트 인식률: 표 내부의 금융 데이터(계좌번호, 이자율 등) 오인식률 분석.
Phase 3: 지능형 전후처리 및 도메인 최적화
 * 목표: 성능 확보 시, SLM과 정규표현식을 조합하여 잔여 오류 해결.
 * 주요 작업:
   * Local SLM(Gemma, Llama 등) 도입: 외부 API 유출 없이 사내 인프라(On-premise)에서 구동하여 데이터 보안 준수.
   * 정의역(Domain Knowledge) 필터: 이자율(*.*%), 날짜(YYYYMMDD) 등 특정 포맷에 대한 Regex(정규표현식) 검증 로직 추가.
   * OCR 결과가 "닫뇨"인 경우 SLM을 통해 "담보"로 자동 교정하는 제로샷(Zero-shot) 파이프라인 구축.
Phase 4: 기술 스택 피벗 (Critical Decision)
 * 목표: PaddleOCR의 한계 도달 시 차세대 아키텍처로 전환.
 * 결정 조건:
   * PP-Structure 및 SLM 보정 후에도 금융 데이터의 정확도가 목표치(예: 98% 이상)에 미달할 경우.
   * 표 구조의 복잡도가 PaddleOCR의 수용 범위를 넘어설 경우.
 * 대안 기술:
   * VLM(Vision-Language Model) 도입: DeepSeek-OCR, Qwen2.5-VL 등 오픈소스 VLM을 로컬 서버에 구축하여 이미지에서 직접 정규화된 JSON 데이터 추출.
3. 기술적 준수 사항 및 제약 조건
| 항목 | 준수 사항 |
|---|---|
| 보안 (Security) | 데이터 외부 유출 금지. Google OCR 등 외부 API 사용 절대 불가. 모든 모델은 로컬 구동 원칙. |
| 효율성 (Efficiency) | 오버 엔지니어링 경계. 표 영역 외 데이터는 과감히 배제하여 연산 자원 및 속도 최적화. |
| 정확도 (Quality) | 이자율, 금액 등 수치 데이터의 무결성 최우선. 오인식 시 PENDING_REVIEW 플래그 설정 필수. |
4. 기대 효과
 * 구조적 안정성: PP-Structure 도입으로 컬럼 밀림 현상을 근본적으로 해결.
 * 데이터 신뢰도: SLM과 Regex의 조화를 통해 휴먼 에러 수준의 정제 데이터 확보.
 * 미래 기술 선점: PaddleOCR의 한계를 명확히 정의하고 필요 시 신속하게 VLM 아키텍처로 전환 가능한 유연성 확보.